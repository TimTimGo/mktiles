cmake_minimum_required(VERSION 2.8)
project( tiles )
set(CMAKE_CXX_FLAGS "-ggdb -g -std=c++14")
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( mktiles main.cpp )
target_link_libraries( mktiles ${OpenCV_LIBS} )
